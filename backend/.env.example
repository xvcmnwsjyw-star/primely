PORT=5000
MONGO_URI=mongodb://localhost:27017/primely
JWT_SECRET=replace_with_a_long_random_string
JWT_EXPIRES_IN=7d
CLIENT_URL=http://localhost:5173

# Stripe (test mode keys from https://dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY=sk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me

# Only needed if you want to trigger `npm run seed`'s logic remotely via
# GET /api/dev/seed?key=... — useful on hosts whose free tier has no shell.
# Leave unset and the route stays disabled.
SEED_SECRET=letmeseed2026
